print("this should show in the console")

function test() {
    print("this should should in the browser")
}

http.handle("/hello", function() {
    print("hello world")
    test()
})

http.listen(3000, function() {
    print("Server started at http://localhost:3000 🌱")
})