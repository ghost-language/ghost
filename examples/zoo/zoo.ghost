import Animal from "animal"
import Person from "person"

class Zoo {
    function visit() {
        lion := Animal("Lion")
        visitor := Person()

        lion.speak()
        visitor.speak()
    }
}