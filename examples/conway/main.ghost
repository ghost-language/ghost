import Game from 'game'

game := Game()

window.create(800, 600, "Conway's Game of Life")
window.setTargetFPS(60)
// window.toggleFullscreen()

while (!window.shouldClose()) {
    game.update()

    game.draw()
}

window.close()