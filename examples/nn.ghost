/* This is a very simple neural network implementation. */
let inputs = [1, 2, 3];
let weights = [0.2, 0.8, -0.5];
let bias = 2;

let output = (inputs[0] * weights[0]) + (inputs[1] * weights[1]) + (inputs[2] * weights[2]);

print(output);