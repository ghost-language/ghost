import Game from 'game'

game := Game(1280, 720)

window.create(game.getWidth(), game.getHeight(), "Snake")
window.setTargetFPS(60)

while (!window.shouldClose()) {
    game.update()


    draw.begin()

    draw.clearBackground()

    game.draw()

    draw.end()
}

window.close()