class Foo {
    bar() {
        return "bar!!!!!!!!!"
    }

    greet(name) {
        if (name) {
            return "Hello, " + name + "..."
        }

        return "Hello, world."
    }
}

foo := Foo()

print foo.greet("Kai")
print foo.greet()