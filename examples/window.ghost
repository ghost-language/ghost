console.log("window demo")

window.create(800, 600, "window demo")
window.setTargetFPS(60)

while (!window.shouldClose()) {
    draw.begin()

    draw.clearBackground()
    text.draw("Hello world", 190, 200, 20)

    draw.rectangle(100, 100, 32, 32)

    draw.end()
}

window.close()