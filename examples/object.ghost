year := 2021

function Person(name, yob) {
    self := {}

    self.name := name
    self.yob := yob
    self.age := year - yob

    return self
}

person := Person("Kai", 1989)
print(person.name)
print(person.yob)
print(person.age)